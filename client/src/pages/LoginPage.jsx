import React from 'react';
import Login from '../components/Login';

const LoginPage = () => {
    return (
        <div className="bg-gradient-to-t from-teal-400 to-teal-700 min-h-screen flex justify-center items-center overflow-hidden">
            <Login />
        </div>
    );
};

export default LoginPage;
